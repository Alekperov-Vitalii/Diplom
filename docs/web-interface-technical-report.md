# Технічна реалізація веб-інтерфейсу системи моніторингу та керування охолодженням GPU-кластера

## 1. Вступ та архітектура файлової системи

Веб-інтерфейс для системи моніторингу та керування охолодженням GPU-кластера було реалізовано як окремий модуль проекту, створений на базі фреймворку Next.js версії 16.0.6 з використанням React 19.2.0 та TypeScript. Директорія `web-interface` була ініціалізована за допомогою стандартного інструменту `create-next-app`, що забезпечує базову структуру проекту з оптимальною конфігурацією для розробки сучасних веб-додатків.

Архітектура файлової системи веб-інтерфейсу організована за принципами App Router, що є стандартом для Next.js 13+ та надає зручну організацію маршрутизації та компонентів. Структура директорії включає наступні ключові елементи:

```
web-interface/
├── app/                    # Директорія App Router Next.js
│   ├── page.tsx           # Головна сторінка (Dashboard)
│   ├── layout.tsx         # Кореневий layout компонент
│   ├── globals.css        # Глобальні стилі (Tailwind CSS)
│   ├── control/           # Сторінка ручного керування
│   │   └── page.tsx
│   └── history/           # Сторінка історії та графіків
│       └── page.tsx
├── components/            # React компоненти
│   └── FanCard.tsx       # Компонент відображення картки вентилятора
├── lib/                  # Бібліотека допоміжних функцій
│   └── api.ts            # API-клієнт для зв'язку з fog-сервером
├── public/               # Статичні ресурси
├── package.json          # Залежності проекту
├── tsconfig.json         # Конфігурація TypeScript
└── next.config.ts        # Конфігурація Next.js
```

Така організація забезпечує чітке розділення відповідальності: `app/` містить маршрути та сторінки, `components/` — переіспользувані UI-компоненти, а `lib/` — бізнес-логіку та утиліти для роботи з API.

## 2. Створення проекту та базова конфігурація

Проект було створено командою `npx create-next-app@latest web-interface` з вибором TypeScript, ESLint, Tailwind CSS та App Router. Після ініціалізації було додано ключові залежності для реалізації функціональності системи:

- **axios** (^1.13.2) — HTTP-клієнт для асинхронних запитів до API
- **recharts** (^3.5.1) — бібліотека для візуалізації даних у вигляді графіків
- **lucide-react** (^0.555.0) — набір SVG іконок для UI
- **date-fns** (^4.1.0) — утиліти для роботи з датами та часом

Конфігурація TypeScript (`tsconfig.json`) налаштована з суворою типізацією (`strict: true`) та підтримкою шляхів через аліас `@/*`, що дозволяє використовувати абсолютні імпорти замість відносних шляхів. Next.js конфігурація включає підтримку React Compiler для оптимізації продуктивності.

## 3. Архітектура обміну даними між компонентами системи

Система моніторингу реалізує багаторівневу архітектуру обміну даними, де інформація проходить наступний шлях:

**InfluxDB → fog-сервер → веб-інтерфейс**

### 3.1. Збір даних та збереження в InfluxDB

Дані про температури GPU, стан вентиляторів та температуру приміщення надходять від апаратного рівня (ESP32 або емулятора) до fog-сервера через HTTP POST запит на ендпоінт `/api/v1/telemetry`. Fog-сервер, отримуючи телеметрію, миттєво зберігає її в InfluxDB за допомогою InfluxDB Client API. Кожна точка даних записується як measurement з відповідними полями (fields) та тегами (tags), що дозволяє ефективно виконувати запити для аналізу та візуалізації.

### 3.2. API fog-сервера для веб-інтерфейсу

Fog-сервер надає RESTful API для веб-інтерфейсу, реалізоване на базі FastAPI. Ключові ендпоінти включають:

- `GET /api/v1/current-state` — отримання поточного стану системи (температури GPU, активні алерти)
- `GET /api/v1/history?hours=N` — історія температур за вказаний період
- `GET /api/v1/fan-history?hours=N` — історія роботи вентиляторів (PWM, RPM)
- `GET /api/v1/fan-statistics` — статистика роботи вентиляторів за останню годину
- `GET /api/v1/system-mode` — поточний режим роботи (автоматичний/ручний)
- `POST /api/v1/system-mode` — зміна режиму роботи
- `POST /api/v1/fan-control/manual` — встановлення ручного керування вентиляторами

Всі запити повертають дані у форматі JSON з чітко визначеною структурою, що забезпечує типобезпеку на рівні TypeScript.

### 3.3. Реалізація API-клієнта в веб-інтерфейсі

API-клієнт реалізовано в файлі `lib/api.ts` та інкапсулює всю логіку взаємодії з fog-сервером. Базова URL API конфігурується через змінну оточення `NEXT_PUBLIC_API_URL` (за замовчуванням `http://localhost:8001`), що дозволяє легко адаптувати систему для різних середовищ розгортання.

Основними функціями API-клієнта є:

- `getCurrentState()` — асинхронна функція, що виконує GET-запит до `/api/v1/current-state` та повертає об'єкт типу `CurrentState` з поточними температурами GPU, списком алертів та timestamp останнього оновлення
- `getHistory(hours)` — отримання історичних даних про температури за вказаний період, результат обробляється та групується на рівні компонентів для візуалізації
- `getFanStatistics()` — запит статистики вентиляторів, що включає середній, мінімальний та максимальний PWM за останню годину, час роботи на високих оборотах та поточні значення RPM та PWM
- `getSystemMode()` та `setSystemMode(mode)` — управління режимом роботи системи
- `setManualFanControl(commands)` — надсилання команд ручного керування вентиляторами, де кожна команда містить `fan_id` та `pwm_duty`

Всі функції реалізовані з обробкою помилок через механізм винятків JavaScript, що дозволяє компонентам реагувати на відсутність зв'язку з сервером або інші проблеми мережевого рівня.

**Листинг 3.1** — Фрагмент API-клієнта для отримання поточного стану та історії:

```typescript
// lib/api.ts (фрагмент)

const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8001';

export interface CurrentState {
  gpu_temps: GPUTemp[];
  alerts: Alert[];
  timestamp: string;
}

export async function getCurrentState(): Promise<CurrentState> {
  const response = await fetch(`${API_BASE_URL}/api/v1/current-state`);
  if (!response.ok) throw new Error('Failed to fetch current state');
  return response.json();
}

export async function getHistory(hours: number = 1): Promise<HistoryDataPoint[]> {
  const response = await fetch(`${API_BASE_URL}/api/v1/history?hours=${hours}`);
  if (!response.ok) throw new Error('Failed to fetch history');
  const json = await response.json();
  return json.data;
}
```

## 4. Реалізація механізму оновлення даних

Для забезпечення реального часу моніторингу системи реалізовано механізм періодичного опитування (polling) fog-сервера. На головній сторінці Dashboard (`app/page.tsx`) використовується React хук `useEffect` з інтервалом оновлення 5 секунд, що забезпечує актуальність даних без надмірного навантаження на мережу та сервер.

Механізм polling реалізовано через `setInterval`, що викликає асинхронну функцію `fetchData()`. Ця функція виконує паралельні запити до кількох ендпоінтів за допомогою `Promise.all()`, що дозволяє одночасно отримувати поточний стан, статистику вентиляторів та режим роботи системи, мінімізуючи затримку оновлення інтерфейсу.

Додатково реалізовано функцію `healthCheck()`, яка виконує запит до ендпоінту `/health` для перевірки доступності fog-сервера. У випадку недоступності сервера користувачу відображається повідомлення про помилку з інструкцією для усунення проблеми.

**Листинг 4.1** — Реалізація механізму polling на головній сторінці:

```typescript
// app/page.tsx (фрагмент)

useEffect(() => {
  const fetchData = async () => {
    try {
      const online = await healthCheck();
      setServerOnline(online);
      
      if (online) {
        const [data, stats, mode] = await Promise.all([
          getCurrentState(),
          getFanStatistics(),
          getSystemMode()
        ]);
        setState(data);
        setFanStats(stats);
        setSystemMode(mode);
      }
    } catch (error) {
      console.error('Error fetching data:', error);
      setServerOnline(false);
    } finally {
      setLoading(false);
    }
  };

  fetchData();
  const interval = setInterval(fetchData, 5000);
  return () => clearInterval(interval);
}, []);
```

## 5. Головна сторінка Dashboard

Головна сторінка (`app/page.tsx`) реалізована як клієнтський компонент (директива `'use client'`) та відображає комплексну інформацію про стан системи охолодження. Інтерфейс організовано у вигляді карток (cards), що групують інформацію за логічними блоками:

1. **Статус-бар** — відображає загальний статус системи, поточний режим роботи (автоматичний/ручний) та timestamp останнього оновлення
2. **Система алертів** — динамічне відображення критичних та попереджувальних повідомлень про температури GPU, що перевищують порогові значення
3. **Картки GPU** — візуалізація температур кожного GPU з кольоровою індикацією стану (зелений — норма, жовтий — підвищена температура, червоний — критична температура)
4. **Картки вентиляторів** — інтеграція компонента `FanCard` для відображення детальної інформації про кожен вентилятор

Для стилізації використовується Tailwind CSS з адаптивною сіткою `grid`, що забезпечує коректне відображення на різних розмірах екранів (responsive design). Кольорова індикація реалізована через умовне застосування CSS-класів на основі значень температур та PWM.

**Листинг 5.1** — Фрагмент відображення карток GPU з умовною стилізацією:

```typescript
// app/page.tsx (фрагмент)

{state?.gpu_temps.map(gpu => {
  const temp = gpu.temperature;
  const isHot = temp >= 90;
  const isWarm = temp >= 70;
  
  return (
    <div
      key={gpu.gpu_id}
      className={`bg-white rounded-lg shadow p-6 border-l-4 ${
        isHot ? 'border-red-500' : 
        isWarm ? 'border-yellow-500' : 'border-green-500'
      }`}
    >
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-bold">GPU {gpu.gpu_id}</h3>
        <Thermometer className={`w-6 h-6 ${
          isHot ? 'text-red-500' : 
          isWarm ? 'text-yellow-500' : 'text-green-500'
        }`} />
      </div>
      <div className="text-3xl font-bold mb-1">
        {temp.toFixed(1)}°C
      </div>
    </div>
  );
})}
```

## 6. Компонент FanCard для відображення статистики вентиляторів

Компонент `FanCard` (`components/FanCard.tsx`) інкапсулює логіку відображення детальної інформації про окремий вентилятор. Компонент отримує пропси типу `FanStatistics`, що містить поточні та історичні дані, температуру відповідного GPU та режим роботи системи.

Ключові елементи компонента:

- **Візуальна індикація PWM** — прогрес-бар з кольоровим кодуванням (зелений для низького PWM, жовтий для середнього, червоний для високого)
- **Поточні значення** — відображення RPM та PWM у реальному часі
- **Статистика за годину** — середній PWM за останню годину та час роботи на високих оборотах (>80% PWM)
- **Контекстна інформація** — температура GPU, до якого прив'язаний вентилятор, та індикація режиму керування

Компонент реалізовано з використанням функціонального підходу та хуків React, що забезпечує оптимальну продуктивність та легкость тестування.

**Листинг 6.1** — Реалізація компонента FanCard:

```typescript
// components/FanCard.tsx (фрагмент)

export default function FanCard({ stats, gpuTemp, mode }: FanCardProps) {
  const { fan_id, current_pwm, current_rpm, avg_pwm_last_hour, time_on_high } = stats;
  
  const getStatusColor = (pwm: number) => {
    if (pwm < 40) return 'text-green-500';
    if (pwm < 70) return 'text-yellow-500';
    return 'text-red-500';
  };
  
  return (
    <div className="bg-white rounded-lg shadow-md p-5 border-l-4 border-blue-500">
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-bold">Вентилятор {fan_id}</h3>
        <span className={`text-sm font-medium ${
          mode === 'auto' ? 'text-blue-600' : 'text-purple-600'
        }`}>
          {mode === 'auto' ? '🤖 Авто' : '🎛️ Ручний'}
        </span>
      </div>
      
      <div className="mb-3">
        <div className="w-full bg-gray-200 rounded-full h-3">
          <div
            className={`h-3 rounded-full transition-all duration-300 ${
              current_pwm < 40 ? 'bg-green-500' :
              current_pwm < 70 ? 'bg-yellow-500' : 'bg-red-500'
            }`}
            style={{ width: `${current_pwm}%` }}
          />
        </div>
      </div>
      
      <div className="flex items-center justify-between">
        <span className="text-sm text-gray-800">Оберти</span>
        <span className="text-sm font-semibold">{current_rpm.toLocaleString()} RPM</span>
      </div>
    </div>
  );
}
```

## 7. Сторінка історії та графіків

Сторінка історії (`app/history/page.tsx`) забезпечує візуалізацію динаміки змін температур та роботи вентиляторів у часі. Для візуалізації використовується бібліотека Recharts, що надає високопродуктивні компоненти для роботи з графіками на базі D3.js.

Реалізовано два основні типи графіків:

1. **Графік температур GPU** — лінійний графік, що відображає температури всіх восьми GPU та температуру приміщення. Кожен GPU має унікальний колір для легкої ідентифікації, а температура приміщення відображається пунктирною чорною лінією
2. **Графік роботи вентиляторів** — перемикається між відображенням PWM (у відсотках) та RPM (оберти на хвилину). Використовується ступінчастий тип лінії (`type="stepAfter"`), що відображає дискретні зміни керуючих команд

Дані для графіків обробляються через функції групування, що перетворюють масив точок даних з InfluxDB у структуру, сумісну з форматом Recharts. Групування виконується за часом з форматуванням у локальний формат часу (година:хвилина).

Реалізовано вибір періоду відображення (1, 3, 6 або 24 години), що дозволяє аналізувати як короткострокові зміни, так і довгострокові тренди. При зміні періоду автоматично виконується новий запит до API з оновленням даних.

**Листинг 7.1** — Фрагмент обробки даних для графіка температур:

```typescript
// app/history/page.tsx (фрагмент)

const tempHistory = await getHistory(hours);

const tempGrouped = tempHistory.reduce((acc: Record<string, ChartDataPoint>, point: HistoryDataPoint) => {
  const time = new Date(point.time).toLocaleTimeString('uk-UA', { 
    hour: '2-digit', 
    minute: '2-digit' 
  });
  
  if (!acc[time]) {
    acc[time] = { time };
  }
  
  if (point.measurement === 'gpu_temps' && point.gpu_id) {
    acc[time][`GPU ${point.gpu_id}`] = point.value;
  } else if (point.measurement === 'room_temp') {
    acc[time]['Кімната'] = point.value;
  }
  
  return acc;
}, {});

setTempData(Object.values(tempGrouped));
```

## 8. Сторінка ручного керування

Сторінка ручного керування (`app/control/page.tsx`) забезпечує інтерфейс для прямого втручання користувача в роботу системи охолодження. Функціональність включає:

### 8.1. Перемикання режимів роботи

Реалізовано перемикач між автоматичним та ручним режимами з візуальною індикацією поточного стану. При перемиканні виконується POST-запит до `/api/v1/system-mode`, що змінює глобальний режим роботи на fog-сервері. У автоматичному режимі система працює під керуванням каскадного алгоритму, а у ручному режимі використовуються команди, встановлені користувачем.

### 8.2. Ручне налаштування PWM вентиляторів

Для кожного з восьми вентиляторів надається слайдер (range input) для встановлення PWM у діапазоні від 0 до 100%. Інтерфейс показує поточну температуру відповідного GPU та попереджувальні повідомлення, якщо температура висока, а PWM встановлено на низькому рівні. Це допомагає уникнути перегріву через помилки користувача.

Реалізовано попередньо встановлені профілі ("Тихий режим" — 25%, "Збалансований" — 50%, "Максимум" — 100%), що дозволяють швидко застосувати стандартні налаштування для всіх вентиляторів одночасно.

### 8.3. Застосування команд та валідація

При натисканні кнопки "Застосувати зміни" система перевіряє, чи активний ручний режим, та виконує валідацію наявності потенційно небезпечних комбінацій (висока температура GPU з низьким PWM). Якщо виявлено попередження, користувачу показується діалог підтвердження перед надсиланням команд.

Команди надсилаються у форматі масиву об'єктів `ManualControlCommand[]`, де кожен об'єкт містить `fan_id` та `pwm_duty`. Fog-сервер зберігає ці команди та застосовує їх при наступному надходженні телеметрії від ESP32.

**Листинг 8.1** — Реалізація застосування ручних команд з валідацією:

```typescript
// app/control/page.tsx (фрагмент)

const handleApplyManualControl = async () => {
  if (mode?.mode !== 'manual') {
    alert('Спочатку переключіться на ручний режим!');
    return;
  }

  const warnings = [];
  for (let i = 1; i <= 8; i++) {
    const gpuTemp = state?.gpu_temps.find(g => g.gpu_id === i)?.temperature || 0;
    const pwm = fanPWM[i];
    
    if (gpuTemp > 70 && pwm < 60) {
      warnings.push(`GPU ${i}: ${gpuTemp.toFixed(1)}°C, але PWM лише ${pwm}%`);
    }
  }

  if (warnings.length > 0) {
    const confirmed = confirm('⚠️ ПОПЕРЕДЖЕННЯ:\n\n' + warnings.join('\n') + '\n\nПродовжити?');
    if (!confirmed) return;
  }

  const commands = Object.keys(fanPWM).map(fanId => ({
    fan_id: parseInt(fanId),
    pwm_duty: fanPWM[parseInt(fanId)]
  }));

  await setManualFanControl(commands);
  alert('✓ Ручні команди застосовано успішно!');
};
```

### 8.4. Історія дій користувача

Сторінка відображає останні дії користувача, отримані через ендпоінт `/api/v1/user-actions`. Це дозволяє відстежувати історію змін режимів та ручних команд для аналізу та діагностики.

## 9. Висновки

Веб-інтерфейс реалізовано як сучасний, адаптивний веб-додаток на базі Next.js з використанням TypeScript для забезпечення типобезпеки та підтримуваності коду. Архітектура системи забезпечує чітке розділення відповідальності між компонентами, що спрощує розробку та тестування.

Механізм обміну даними через RESTful API забезпечує надійну інтеграцію з fog-сервером, а використання polling з інтервалом 5 секунд надає прийнятний баланс між актуальністю даних та навантаженням на систему. Візуалізація даних через Recharts дозволяє ефективно аналізувати роботу системи охолодження та робити обґрунтовані рішення щодо оптимізації.

Реалізація ручного режиму керування забезпечує гнучкість системи для тестування, діагностики та спеціальних сценаріїв використання, водночас зберігаючи безпеку через систему попереджень та валідації.

